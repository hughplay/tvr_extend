# @package _global_
defaults:
  - override /dataset: view
  - override /model: detector
  - override /evaluator: view
  - override /optim: adamw
  - override /scheduler: step
  - override /recorder: view
  - override /callbacks: [checkpoint, lr_monitor, early_stopping]

dataset:
  use_box: true