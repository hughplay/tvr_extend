# @package _global_
defaults:
  - override /dataset: event
  - override /model: concat_resnet
  - override /evaluator: event
  - override /optim: adamw
  - override /scheduler: step
  - override /recorder: event
  - override /callbacks: [checkpoint, lr_monitor, early_stopping]