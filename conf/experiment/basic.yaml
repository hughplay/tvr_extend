# @package _global_
defaults:
  - override /dataset: basic
  - override /model: single_concat_resnet
  - override /evaluator: basic
  - override /optim: adamw
  - override /scheduler: step
  - override /recorder: basic
  - override /callbacks: [checkpoint, lr_monitor]